server:
  port: 8082

spring:
  application:
    name: miniDoorayTaskAPI
  datasource:
    url: jdbc:mysql://220.67.216.14:13306/nhn_academy_8?serverTimezone=Asia/Seoul
    username: nhn_academy_8
    password: zq#ueLhsLMT258C?
    driver-class-name: com.mysql.cj.jdbc.Driver

  # JPA/Hibernate ??
  jpa:
    hibernate:
      # ddl-auto: Spring Boot? ??? ? ?????? ???? ??? ???? ??
      # update: ??? ?? ??? DB? ?? (?? ???? ?? ?? ??)
      # none: ???? ? ? (?? ?? ??)
      ddl-auto: update
    show-sql: true # ???? SQL ??? ??? ?? (?? ? ??)
    properties:
      hibernate:
        format_sql: true # SQL ??? ?? ?? ??

#  cloud:
#    gateway:
#      routes:
#        - id: task_route
#          # ?????? Gateway? "/api/tasks/..." ??? ?? ?
#          uri: http://220.67.223.61:8082 # TaskAPI? ?? ?? ? ??
#          predicates:
#            - Path=/tasks_api/** # ? ??? ??? ??? TaskAPI? ??
#          # (??) Path=/api/tasks/** ???? /api/tasks ??? ???? ??
#          filters:
#            - RewritePath=/tasks_api/(?<remaining>.*), /${remaining}
#
#    compatibility-verifier:
#      enabled: false
#
#  main:
#    web-application-type: reactive